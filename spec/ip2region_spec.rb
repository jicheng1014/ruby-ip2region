# frozen_string_literal: true

RSpec.describe Ip2region do
  it "has a version number" do
    expect(Ip2region::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
